# Руководство по внесению вклада

Спасибо за интерес к проекту Gmail Subscription Cleaner! Мы приветствуем любой вклад.

## Как внести вклад

### Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте, не была ли она уже сообщена в Issues
2. Если нет, создайте новый Issue с:
   - Четким описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым и фактическим поведением
   - Версией Chrome и ОС
   - Скриншотами (если применимо)

### Предложение новых функций

1. Создайте Issue с меткой "enhancement"
2. Опишите предлагаемую функцию
3. Объясните, почему она будет полезна
4. Дождитесь обсуждения перед началом разработки

### Pull Requests

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Следуйте правилам кодирования проекта (см. `.cursorrules`)
4. Коммитьте изменения (`git commit -m 'Add amazing feature'`)
5. Пушьте в ветку (`git push origin feature/amazing-feature`)
6. Откройте Pull Request

## Правила кодирования

### Общие принципы

- Следуйте принципам SOLID и KISS
- Пишите чистый, читаемый код
- Добавляйте комментарии для сложной логики
- Используйте осмысленные имена переменных и функций

### JavaScript

- Используйте ES6+ синтаксис
- Используйте `const` и `let`, избегайте `var`
- Используйте arrow functions где уместно
- Именование: camelCase для переменных/функций, PascalCase для классов
- Используйте точку с запятой
- Отступы: 2 пробела
- Максимальная длина строки: 100 символов

### Комментарии

- Комментарии на русском языке
- JSDoc для публичных функций
- Inline комментарии для сложной логики

Пример JSDoc:
```javascript
/**
 * Получение списка писем с пагинацией
 * @param {object} params - параметры запроса
 * @returns {Promise<object>}
 */
async getMessages(params = {}) {
  // ...
}
```

### Структура кода

- Один файл = одна ответственность
- Разделение на модули: background, popup, utils
- Асинхронная работа через async/await
- Обработка всех ошибок

### Git

- Коммиты на русском или английском языке
- Используйте осмысленные сообщения коммитов
- Один коммит = одно логическое изменение
- Формат: `Тип: Краткое описание`

Типы коммитов:
- `Feat:` - новая функция
- `Fix:` - исправление ошибки
- `Docs:` - изменения в документации
- `Style:` - форматирование кода
- `Refactor:` - рефакторинг
- `Test:` - добавление тестов
- `Chore:` - обновление зависимостей, настройки

Примеры:
```
Feat: Добавлена виртуализация списков
Fix: Исправлена ошибка при удалении писем
Docs: Обновлена инструкция по установке
```

## Тестирование

Перед отправкой PR:

1. Протестируйте на реальном Gmail аккаунте
2. Проверьте работу на разных объемах писем
3. Убедитесь, что нет ошибок в Console
4. Проверьте на разных разрешениях экрана
5. Протестируйте edge cases

## Обработка ошибок

- Всегда обрабатывайте ошибки
- Показывайте понятные сообщения пользователю
- Логируйте технические детали в console
- Используйте try-catch для асинхронных операций

## Gmail API

- Соблюдайте rate limits (250 quota units/user/second)
- Используйте batch requests где возможно
- Кэшируйте результаты в chrome.storage.local
- Обрабатывайте ошибки 401, 403, 429, 500

## UI/UX

- Минималистичный и понятный интерфейс
- Подтверждение перед деструктивными операциями
- Показывайте прогресс для длительных операций
- Визуальная обратная связь на все действия

## Безопасность

- Никогда не удаляйте письма без подтверждения
- Белый список защищает важные письма
- Не логируйте содержимое писем
- Храните минимум данных локально

## Производительность

- Виртуализация списков при >100 элементах
- Debounce для поиска (300ms)
- Lazy loading деталей писем
- Оптимизация DOM операций

## Что НЕ делать

- ❌ Не использовать jQuery или тяжелые библиотеки
- ❌ Не хранить OAuth токены в небезопасных местах
- ❌ Не делать синхронные операции в UI потоке
- ❌ Не игнорировать ошибки API
- ❌ Не удалять письма без согласия пользователя
- ❌ Не коммитить конфиденциальные данные

## Вопросы?

Если у вас есть вопросы:
1. Проверьте документацию (README.md, SETUP.md)
2. Поищите в Issues
3. Создайте новый Issue с вопросом

Спасибо за ваш вклад! 🚀

